# ProducerBuddy 🎵

An iOS app for music producers to store, organize, and sequence their tracks —
then share them cleanly. Built for **iOS 18** with **SwiftUI**, **SwiftData**,
and **Swift Testing**, with the project generated by **XcodeGen**.

## What it does

ProducerBuddy follows a producer's workflow from raw idea to release:

- **Library** — Import audio mixes, tag each song with BPM, key, genre, a
  workflow category (idea → demo → WIP → ready to mix → mastered → released),
  star rating, and notes. Search, sort, and filter by category.
- **Multiple mixes per song** — Keep every version of a track (rough mix,
  master v2, …), mark one as the "primary", and A/B them with the built-in
  player.
- **Playback** — A persistent now-playing bar with scrub and play/pause sits
  above the tab bar.
- **Projects (albums / EPs / singles / mixtapes)** — Group songs into a release
  and design the running order.
- **Sequencing engine** — For each track the app reports an energy **Rise**,
  **Fall**, or **Steady** move relative to the previous track (using BPM as an
  energy proxy), and warns about abrupt BPM jumps. "Suggest Order" arranges the
  record to build energy and peak before the wind-down.
- **Sharing** — Share a song's marketing blurb or a project's tracklist via the
  system share sheet.

## Project layout

```
project.yml                     XcodeGen project definition (iOS 18 target + tests)
Sources/
  ProducerBuddyApp.swift        App entry point + SwiftData container
  Models/                       SwiftData @Model types + supporting enums
  Services/                     SequencingEngine, AudioStorage, AudioPlayer
  Views/                        Library and Projects screens
  Components/                   Reusable view pieces (rows, badges, now-playing)
Tests/                          Swift Testing suites
```

## Building

This repo ships the XcodeGen spec rather than a checked-in `.xcodeproj`:

```bash
brew install xcodegen      # if you don't have it
xcodegen generate          # creates ProducerBuddy.xcodeproj
open ProducerBuddy.xcodeproj
```

Then build/run the **ProducerBuddy** scheme on an iOS 18 simulator. Run tests
with ⌘U (the **ProducerBuddyTests** target uses Swift Testing).

## Design notes

- The **SequencingEngine** is intentionally pure (no SwiftData / UIKit imports)
  so the rise/fall and order-suggestion logic is fully unit-tested.
- Imported audio is copied into the app's `Documents/Audio` directory and
  referenced by relative filename, so records survive across launches.
- This is a hobby-jam scope; stretch ideas from the brief (EQ/mastering, stem
  splitting, pitch/BPM editing, direct social posting) are left as future work.
